StylesPath = .vale/styles
MinAlertLevel = suggestion

[docs/**/*.md]
BasedOnStyles = GitSwitchboard
# Ignore inline code to avoid false positives
TokenIgnores = (`[^`]+`)

# Internal design docs and plans are not user-facing
[docs/plans/**/*.md]
BasedOnStyles =

[README.md]
BasedOnStyles = GitSwitchboard
TokenIgnores = (`[^`]+`)
